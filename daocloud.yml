image: daocloud/ci-python:2.7

services:

env:

install:
    - echo "Begin to install dependencies for testing"
    - echo "Running pip install -r requirements.txt"
    - pip install -r test_requirements.txt

before_script:

script:
    - echo "Begin to run unit tests with code coverage"
    - echo "Running nosetests -w test --with-coverage" 
    - nosetests -w test --with-coverage
	